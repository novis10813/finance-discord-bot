# Discord Bot 設定檔範例
# 複製此檔案為 .env 並填入實際值

# Discord Bot Token (必填)
# 從 https://discord.com/developers/applications 取得
DISCORD_BOT_TOKEN=your_bot_token_here

# 指令前綴 (選填，預設為 !)
DISCORD_COMMAND_PREFIX=!

# Bot 名稱 (選填)
BOT_NAME=Discord Bot

# Bot 描述 (選填)
BOT_DESCRIPTION=A Discord bot built with discord.py

# 日誌級別 (選填，預設為 INFO)
# 可選值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日誌檔案路徑 (選填，預設為 logs/bot.log)
LOG_FILE=logs/bot.log

# Cogs 資料夾路徑 (選填，預設為 cogs)
COGS_DIR=cogs

# 是否自動載入 Cogs (選填，預設為 True)
AUTO_LOAD_COGS=True

# 功能頻道設定
# 每日籌碼分析發送頻道 (Forum Channel ID)
CHIP_CHANNEL_ID=<chip-channel ID>

# 籌碼分析指令限制頻道 (選填，多個頻道用逗號分隔，不設定表示允許所有頻道)
# 範例：CHIP_ALLOWED_CHANNELS=1234567890123456789,9876543210987654321
CHIP_ALLOWED_CHANNELS=

# 金融摘要功能設定
# OpenRouter API Key (必填)
OPENROUTER_API_KEY=your_openrouter_api_key
# OpenRouter Model (選填，預設 google/gemini-2.0-flash-exp:free)
OPENROUTER_MODEL=google/gemini-2.0-flash-exp:free

# YTtranscript API Endpoint (必填, e.g., http://localhost:8000)
YOUTUBE_SOURCE_ENDPOINT=http://localhost:8000

# 金融摘要發送頻道 (選填，若未設定則使用 CHIP_CHANNEL_ID)
FINANCE_CHANNEL_ID=

# Webhook 接收設定 (for source-aggregator)
# Webhook server 監聽的 port
WEBHOOK_PORT=8080
# Webhook 驗證密鑰 (選填，建議設定以增加安全性)
WEBHOOK_SECRET=your_webhook_secret_here
